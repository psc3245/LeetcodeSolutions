class Solution:
    def stoneGame(self, piles: List[int]) -> bool:
        return True
        # Alice can ensure that she can pick either all even or all odd
        # Since total is odd one of those is guaranteed to be greater